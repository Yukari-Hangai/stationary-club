<div>
  <h2>本当に退会しますか？</h2>
  <div>
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div>
    <%= link_to '退会しない', edit_user_path(current_user.id) %>
    <%= link_to "退会する", out_user_path(current_user.id), method: :patch %>
  </div>
</div>